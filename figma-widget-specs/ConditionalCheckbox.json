{
  "type": "ConditionalCheckbox",
  "mappings": {
    "type": "!figma-conditional-checkbox",
    "widgetType": "%nativeType($)"
  },
  "userData": {
    "id": {
      "type": "string",
      "default": null,
      "description": "id to use in forms"
    },
    "push": {
      "type": "string",
      "default": "",
      "description": "The name of the screen to push"
    },
    "popup": {
      "type": "boolean",
      "default": false,
      "description": "Should the screen be pushed as a popup"
    },
    "state": {
      "type": "string",
      "default": "state",
      "description": "The key of the variants that represent the different states"
    },
    "checked": {
      "type": "string",
      "default": "checked",
      "description": "The name of the variant that represents a \"checked\" state"
    },
    "unchecked": {
      "type": "string",
      "default": "unchecked",
      "description": "The name of the variant that represents an \"unchecked\" state"
    }
  }
}